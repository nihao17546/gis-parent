<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yugoo.gis.dao.ConsumerInfoDAO">
    <select id="selectCount" resultType="java.lang.Long">
        select count(1) from tb_customer_info where 1=1
        <if test="startTime != null">
            and ctime &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            and ctime &lt;= #{endTime}
        </if>
    </select>
    <select id="select" resultType="com.yugoo.gis.pojo.po.ConsumerInfoPO">
        select * from tb_customer_info where 1=1
        <if test="startTime != null">
            and ctime &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            and ctime &lt;= #{endTime}
        </if>
        order by id desc
        limit ${offset},${rows}
    </select>
    <delete id="deleteById">
        delete from tb_customer_info where id=#{id}
    </delete>
    <select id="selectByCtime" resultType="com.yugoo.gis.pojo.po.StatisticCustomerPO">
        select userId,count(userId) con from tb_customer_info
        where ctime between #{start} and #{end}
        group by userId
    </select>
    <select id="selectByBookedTime" resultType="com.yugoo.gis.pojo.po.StatisticCustomerPO">
        select userId,count(userId) con from tb_customer_info
        where bookedTime between #{start} and #{end}
        group by userId
    </select>
    <select id="selectByTransactedTime" resultType="com.yugoo.gis.pojo.po.StatisticCustomerPO">
        select userId,count(userId) con from tb_customer_info
        where transactedTime between #{start} and #{end}
        group by userId
    </select>
    <select id="selectByIds" resultType="com.yugoo.gis.pojo.po.ConsumerInfoPO">
        select id,userId,photo,ctime from tb_customer_info
        where id in
        <foreach collection="ids" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
</mapper>